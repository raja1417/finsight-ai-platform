{{- if .Values.secrets.databaseUrl }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "finsight.fullname" . }}
  labels:
    {{- include "finsight.labels" . | nindent 4 }}
type: Opaque
stringData:
  DATABASE_URL: {{ .Values.secrets.databaseUrl | quote }}
{{- end }}
